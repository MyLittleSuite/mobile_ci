name: Publish Flutter Images

on:
  release:
    types: [ published ]
  push:
    branches:
      - develop

jobs:
  publish:
    name: Push Flutter Image to Docker Hub
    runs-on: ${{ matrix.arch == 'x64' && 'ubuntu-24.04' || 'ubuntu-24.04-arm' }}
    strategy:
      matrix:
        api: [34, 35, 36]
        arch: [x64, arm64]
        include:
          - api: 34
            build_tools: "34.0.0"
            cmdtools: 10406996
            dart_version: "3.2.1"
          - api: 35
            build_tools: "35.0.0"
            cmdtools: 13114758
            dart_version: "3.7.3"
          - api: 36
            build_tools: "36.0.0"
            cmdtools: 13114758
            dart_version: "3.8.1"
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4
      - name: Get release name
        uses: olegtarasov/get-tag@v2.1.2
        id: tagName
      - name: Build and push Flutter image
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
          GIT_REF: ${{ github.ref }}
          RELEASE_NAME: ${{ steps.tagName.outputs.tag }}
        run: |
          chmod +x build.sh
          ./build.sh --build --deploy --android-api ${{ matrix.api }} --build-tools ${{ matrix.build_tools }} --cmdtools ${{ matrix.cmdtools }} --dart --dart-arch ${{ matrix.arch }} --dart-version ${{ matrix.dart_version }} --arch ${{ matrix.arch == 'x64' && 'amd64' || 'arm64' }}

  publish-manifest:
    name: Publish Manifest
    runs-on: ubuntu-latest
    needs:
      - publish
    strategy:
      matrix:
        api: [34, 35, 36]
        include:
          - api: 34
            dart_version: "3.2.1"
          - api: 35
            dart_version: "3.7.3"
          - api: 36
            dart_version: "3.8.1"
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4
      - name: Get release name
        uses: olegtarasov/get-tag@v2.1.2
        id: tagName
      - name: Publish Flutter image manifest
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
          GIT_REF: ${{ github.ref }}
          RELEASE_NAME: ${{ steps.tagName.outputs.tag }}
        run: |
          chmod +x manifest.sh
          ./manifest.sh --android-api ${{ matrix.api }} --dart --dart-version ${{ matrix.dart_version }}